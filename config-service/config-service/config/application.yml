spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri:
        bootstrap: true

server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
